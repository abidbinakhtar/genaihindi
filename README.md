# genaihindi

```
conda create -p env python=3.8
source activate ./env
pip list
git status
pip install ipykernel
git status
git add .
git commit -m "Folder Structure Updated"
git config --global user.email "abidbinakhtar@gmail.com"
git config --global user.name "abidbinakhtar"
git commit -m "Folder Structure Updated"
git remote add origin https://github.com/abidbinakhtar/genaihindi.git
git push -f origin main
pip install -r requirements.txt
```

